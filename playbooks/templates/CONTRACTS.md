<!-- Written in Wave 0. FROZEN during the wave — workers consume, never negotiate. -->
# CONTRACTS — Epic <NN>

## Types
<shared data types every story's code must import rather than redefine>

## API endpoints
<method, path, request shape, response shape, for every endpoint a story will call or implement>

## Data schema
<tables/collections, fields, types, relationships>

## Interfaces/signatures
<function and module signatures stories are expected to implement or call>

## Conventions
<naming, error handling, and structural conventions that keep parallel stories consistent>
